# How to Add a Utility Function

When adding a function under `utils`, please follow the rules below:

- **Reuse Rule:** Before creating a new function, check if an existing one already meets your needs.
- **Create Rule:** Define your code within a properly named function using `def [function_name]`.
- **Zero Change Rule:** Do not modify existing utility functions unless you have aligned with the function's owner.
